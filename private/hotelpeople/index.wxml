<view>
	<view class="swiper-tab">
		<view class="swiper-tab-item {{currentTab==1?'active':''}}" data-current="1" bindtap="clickTab">未确认
			<sup wx:if="{{ 99>=wqr.length>0 && wqr.length!==0}}" class="total">{{wqr.length}}</sup>
			<sup wx:elif="{{ wqr.length>99 }}" class="total">99+</sup>
		</view>
		<view class="swiper-tab-item {{currentTab==2?'active':''}}" data-current="2" bindtap="clickTab">未付款
			<sup wx:if="{{ 99>=wfk.length>0 && wfk.length!==0}}" class="total">{{wfk.length}}</sup>
			<sup wx:elif="{{ wfk.length>99 }}" class="total">99+</sup>
		</view>
    	<view class="swiper-tab-item {{currentTab==3?'active':''}}" data-current="3" bindtap="clickTab">已付款
			<sup wx:if="{{ 99>=yfk.length>0 && yfk.length!==0}}" class="total">{{yfk.length}}</sup>
			<sup wx:elif="{{ yfk.length>99 }}" class="total">99+</sup>
		</view>
    	<view class="swiper-tab-item {{currentTab==4?'active':''}}" data-current="4" bindtap="clickTab">待验收
			<sup wx:if="{{ 99>=dys.length>0 && dys.length!==0}}" class="total">{{dys.length}}</sup>
			<sup wx:elif="{{ dys.length>99 }}" class="total">99+</sup>
		</view>
    	<view class="swiper-tab-item {{currentTab==5?'active':''}}" data-current="5" bindtap="clickTab">已完成
			<sup wx:if="{{ 99>=ywc.length>0 && ywc.length!==0}}" class="total">{{ywc.length}}</sup>
			<sup wx:elif="{{ myOrder>99 }}" class="total">99+</sup>
		</view>

	</view>

</view>

<view wx:if="{{currentTab==1}}">

	<view class="nodd" wx:if="{{unconfirmed.length == 0}}">
		<text>没有订单</text>
	</view>
	<!-- 下面 -->
	<view class="title" wx:for="{{unconfirmed}}" wx:key="unique" wx:for-index="idx">
		<view>
			<view class="title1">
				<view class="content">
				
					<view class="huo">
						<text style="display:flex">订单号：{{item.order_no}}</text>
						<text style="display:flex">下单时间：{{item.add_time}}</text>
							<view class="mydescr">
									<text >{{item.describe}}</text>
							</view>
					
					</view>
					<view class="color" bindtap="detalis" data-id="{{item.id}}" style="margin-top:20rpx;">
						<text>点击查看订单详情</text>
					</view>
					<view class="confirmCancel">
						<view >
							<view class="confirm text" bindtap="confirmOrder" data-id="{{item.id}}">确认订单</view>
							<view class="cancel text" bindtap="cancelOrder" data-id="{{item.id}}">取消订单</view>
						</view>
					</view>
			
					
				
					<view class="invalid">
						<text wx:if="{{item.status == 5}}">已取消</text>
					</view>
				</view>
			</view>
		</view>
	
	</view>
	<view class="bottom">
		<view class="itemed">
			<view class="itemded">
				<button open-type="contact" class="btn">联 系 我 们</button>
			</view>
		</view>
	</view>
</view>
<view wx:if="{{currentTab==2}}">
	<view class="nodd"  wx:if="{{unpaid.length == 0}}">
			<text>没有订单</text>
		</view>
	<!-- 下面 -->
	<view class="title" wx:for="{{unpaid}}" wx:key="unique" wx:for-index="idx">
		<view>
			<view class="title1">
				<view class="content">
				
					<view class="huo">
						<text style="display:flex">订单号：{{item.order_no}}</text>
						<text style="display:flex">下单时间：{{item.add_time}}</text>
					
							<view class="mydescr">
									<text >{{item.describe}}</text>
							</view>
				
					</view>
				<view class="color" bindtap="detalis" data-id="{{item.id}}" style="margin-top:20rpx;">
						<text>点击查看订单详情</text>
					</view>
			
					<!-- <view>{{item.price}}</view> -->
					<view  bindtap="payment" data-id="{{item.id}}" data-num="{{item.items_count}}" data-price="{{item.price}}">
						<text class="payment" >支付</text>
					</view>
					<view>
						<!-- <text class="payment" wx:if="{{item.status == '4'||item.status == '3'||item.status == '2'}}" data-value="{{item}}" bindtap="print">打印</text> -->
					</view>
					<view class="invalid">
						<text wx:if="{{item.status == 5}}">已取消</text>
					</view>
				</view>
			</view>
		</view>
	
	</view>
	<view class="bottom">
		<view class="itemed">
			<view class="itemded">
				<button open-type="contact" class="btn">联 系 我 们</button>
			</view>
		</view>
	</view>
</view>
<view wx:if="{{currentTab==3}}">
	<view  class="nodd" wx:if="{{paid.length == 0}}">
		<text>没有订单</text>
	</view>
	<!-- 下面 -->
	<view class="title" wx:for="{{paid}}" wx:key="unique" wx:for-index="idx">
		<view>
			<view class="title1">
				<view class="content">
				
					<view class="huo">
						
						<text style="display:flex">订单号：{{item.order_no}}</text>
						<text style="display:flex">下单时间：{{item.add_time}}</text>
							<view class="mydescr">
									<text >{{item.describe}}</text>
							</view>
						<view class="zhong" style="position:absolute;top:62rpx;right:10rpx;">
							<text >待处理</text>
						</view>
					</view>
					<view class="color" bindtap="detalis" data-id="{{item.id}}" style="margin-top:20rpx;">
						<text>点击查看订单详情</text>
					</view>
				
					<!-- <view>{{item.price}}</view> -->
					<view wx:if="{{!item.nowRole && item.status != 5}}" bindtap="payment" data-id="{{item.id}}" data-num="{{item.items_count}}" data-price="{{item.price}}">
						<text class="payment" wx:if="{{item.pay_status_name == '待支付'}}">支付</text>
					</view>
					<view>
						<!-- <text class="payment" wx:if="{{item.status == '4'||item.status == '3'||item.status == '2'}}" data-value="{{item}}" bindtap="print">打印</text> -->
					</view>
					<view class="invalid">
						<text wx:if="{{item.status == 5}}">已取消</text>
					</view>
				</view>
			</view>
		</view>
	
	</view>
	<view class="bottom">
		<view class="itemed">
			<view class="itemded">
				<button open-type="contact" class="btn">联 系 我 们</button>
			</view>
		</view>
	</view>
</view>
<view wx:if="{{currentTab==4}}">
	<view class="nodd" wx:if="{{unaccepted.length == 0}}">
		<text>没有订单</text>
	</view>
	<!-- 下面 -->
	<view class="title" wx:for="{{unaccepted}}" wx:key="unique" wx:for-index="idx">
		<view>
			<view class="title1">
				<view class="content">
				
					<view class="huo">
						
						<text style="display:flex">订单号：{{item.order_no}}</text>
						<text style="display:flex">下单时间：{{item.add_time}}</text>
							<view class="mydescr">
									<text >{{item.describe}}</text>
							</view>
						<view class="zhong"  style="position:absolute;top:62rpx;right:10rpx;">
							<text bindtap="receivingGoods" data-id="{{item.id}}">确认收货</text>
							
						</view>
						<text  bindtap="exchange" data-id="{{item.id}}">退换货</text>
					</view>
					<view class="color" bindtap="detalis" data-id="{{item.id}}" data-shouhuo="{{4}}"  style="margin-top:20rpx;">
						<text>点击查看订单详情</text>
					</view>
				
				</view>
			</view>
		</view>
	
	</view>
	<view class="bottom">
		<view class="itemed">
			<view class="itemded">
				<button open-type="contact" class="btn">联 系 我 们</button>
			</view>
		</view>
	</view>
</view>
<view wx:if="{{currentTab==5}}">
		<view class="nodd" wx:if="{{completed.length == 0}}">
		<text>没有订单</text>
	</view>
	<!-- 下面 -->	
	<view class="title" wx:for="{{completed}}" wx:key="unique" wx:for-index="idx">
		<view>
			<view class="title1">
				<view class="content">
				
					<view class="huo">
						
						<text style="display:flex">订单号：{{item.order_no}}</text>
						<text style="display:flex">下单时间：{{item.add_time}}</text>
						<view class="mydescr">
									<text >{{item.describe}}</text>
							</view>
						<view class="zhong"  style="position:absolute;top:62rpx;right:10rpx;">
										<text >完成</text>
						</view>
					</view>
					<view class="color" bindtap="detalis" data-id="{{item.id}}" style="margin-top:20rpx;">
						<text>点击查看订单详情</text>
					</view>
				
					<!-- <view>{{item.price}}</view> -->
					<view wx:if="{{!item.nowRole && item.status != 5}}" bindtap="payment" data-id="{{item.id}}" data-num="{{item.items_count}}" data-price="{{item.price}}">
						<text class="payment" wx:if="{{item.pay_status_name == '待支付'}}">支付</text>
					</view>
					<view>
						<!-- <text class="payment" wx:if="{{item.status == '4'||item.status == '3'||item.status == '2'}}" data-value="{{item}}" bindtap="print">打印</text> -->
					</view>
					<view class="invalid">
						<text wx:if="{{item.status == 5}}">已取消</text>
					</view>
				</view>
			</view>
		</view>
	
	</view>
	<view class="bottom">
		<view class="itemed">
			<view class="itemded">
				<button open-type="contact" class="btn">联 系 我 们</button>
			</view>
		</view>
	</view>
</view>
